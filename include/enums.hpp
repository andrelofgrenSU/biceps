#pragma once

#define SEC_PER_YEAR 3.15576e7
#define PA_TO_MPA 1e-6

enum MIXED_ELEMENTS {
	P1P1,
	P2P1
};

enum FSSA_VERSION {
	FSSA_NONE,
	FSSA_VERTICAL,
	FSSA_NORMAL 
};

enum TIMESTEP_SCHEME {
	EXPLICIT,
	SEMI_IMPLICIT,
	MIDPOINT,
	CRANK_NICOLSON
};

enum JACOBIAN_STENCIL {
	FORWARD,
	CENTER
};

enum VELOCITY_COMPONENT {
	HORIZONTAL,
	VERTICAL
};

enum COORD_SYSTEM {
	CARTESIAN,
	NORMAL_TANGENTIAL
};

enum LINEAR_SOLVER {
	UMFPACK_LU,
	SPARSE_LU,
	BICGSTAB
};
